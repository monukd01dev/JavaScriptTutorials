//function expression is that when we assign a function to a variable it is also known as anoymous functions which is basically a function without a name so it is called by the name of the variable to which function is assigned.

const  happyBirthday = function (name){
    console.log(`happy birthday ${name}`)
}
happyBirthday("monu");

// in above example we have declared a const named as happyBirthday and assigned it with a function